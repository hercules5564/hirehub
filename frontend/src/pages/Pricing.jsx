import PricingSection from '@/components/ui/pricing-section-4';

// The Premium button in the navbar links here (/pricing). Renders the animated
// pricing section (sparkles + scroll-reveal) with HireHub's plans.
const Pricing = () => <PricingSection />;

export default Pricing;
